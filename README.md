# 開店平台 · v0.21

零抽成、年費制的電商開店 SaaS 平台。
（獨立專案，與金融工具無關。）

- **`index.html`**：正式版，資料存在 Supabase 資料庫。
  - 不帶參數：**平台首頁**（介紹、年費、免費開店）
  - `?store=<代號>`：那一家商店的**前台**，例如 `…/?store=demo`
  - `#admin`：**商家後台**（登入後自動進到自己的店）
  - `#platform`：**平台總控台**（只有平台管理者能進）
- **`demo.html`**：離線示範版，資料只存在這個瀏覽器，不用網路也能玩（只有一家店）。

## v0.21 新增：新訂單通知

- 商家後台開著時每 20 秒安全檢查一次新訂單，不用重新整理頁面。
- 有新訂單會在後台顯示單號、顧客姓名與金額；一次多筆會合併提醒。
- 總覽提供「開啟瀏覽器通知」，商家主動允許後，即使正在看其他分頁也能收到系統通知。
- 點系統通知會回到訂單頁；未開啟或封鎖瀏覽器通知時，後台畫面提示仍正常運作。
- 只有具有「訂單」權限的店主或員工能取得通知；API 只回提示必要欄位，不回完整聯絡資料與商品明細。

## v0.20 新增：GitHub Actions 自動測試

- 每次 push、Pull Request 或手動執行時，自動啟動 PostgreSQL 16 並跑 `test/run-all.sh`。
- 測試範圍包含登入、權限、下單、庫存、報表、員工、退款、商品評價、法律文件與資料備份。
- 在 GitHub repository 的 **Actions** 頁面可查看綠勾、失敗項目與完整紀錄。
- 工作流程檔案：`.github/workflows/test.yml`。

## v0.19 新增：資料備份

- **商家備份**：店主可在「商店 → 設定 → 帳號與資料」下載自己的完整 JSON 備份。
- **平台備份**：平台管理者可在「平台設定」下載所有商店與平台資料的完整 JSON 備份。
- 備份包含商品、規格、訂單、退款、會員、庫存異動、進貨、供應商、行銷、評價與設定；平台備份另含收款紀錄。
- 只有店主能匯出自己的店，只有平台管理者能匯出全部；未登入或一般員工無法使用。
- 備份含顧客個資與營運資料，下載後請存放在安全位置。

## v0.18 新增：正式寄信設定包

- 新增 [`docs/寄信設定.md`](docs/寄信設定.md)：Resend 寄件網域、SMTP、Supabase、網址設定與驗收步驟。
- 新增可直接貼進 Supabase 的繁體中文「確認註冊」與「重設密碼」Email 範本。
- SMTP 密碼與 API key 只填在 Supabase 後台，不會存進專案或 GitHub。
- 這一版不會自動啟用寄信；需由專案擁有者完成 Resend 與 Supabase 後台設定。

## v0.17 新增：服務條款、隱私權與退換貨政策

- **平台法律文件**：平台管理者可在「平台設定」編輯服務條款與隱私權政策；平台首頁頁尾提供獨立頁面。
- **同意流程**：商家建立帳號、建立商店，以及顧客註冊會員時，都要先勾選同意服務條款與隱私權政策。
- **商店退換貨政策**：商家可在後台「設定」填寫，顯示在商店頁尾、獨立政策頁與結帳頁。
- 服務條款與隱私權政策各最多 20,000 字；退換貨政策最多 10,000 字，正式版由資料庫再次檢查。

## v0.16 新增：商品評價

- **誰可以評**：買過這個商品、訂單已出貨或已完成的**會員**。一筆訂單的一個商品評一次，之後可以修改。名字會遮成「林**」。
- **在哪裡評**：會員的訂單頁「評價這次買的商品」，或商品頁的「寫評價」。選 1～5 顆星，心得選填（最多 500 字）。
- **前台顯示**：商品頁有平均星等、各星等數量、評價列表（標示「已購買」、店家回覆），一次 10 則可以看更多；商品列表和商品頁價格下方顯示平均星等。
- **後台「商品 → 評價」**：依狀態（待審核／公開中／已隱藏）和星等篩選；可以**回覆**（公開在商品頁）、**隱藏**或重新公開。看得到是哪位會員、哪一筆訂單。
- **設定**：可以關閉評價，或改成「新評價要我先確認才公開」（選單會顯示待審核數量）。被隱藏的評價，顧客修改後還是隱藏。
- 評價管理跟著「商品」權限；評價設定要「設定」權限。

## v0.15 新增：商品管理強化

- **Excel 匯出／匯入**：商品頁「匯出 Excel」拿到目前的商品表（一列一個規格），在 Excel 改價格、貨號、名稱、狀態、分類、描述，或往下新增商品，再按「匯入 Excel」。
  - 匯入前會先**預覽**：新增幾個、更新幾個、改了哪些；有錯會列出第幾列、哪裡錯（例如價格不是數字、分類不存在），全部正確才能匯入。
  - **全部成功才生效**，不會匯到一半。一次最多 500 個商品。支援 .xlsx 和 .csv（UTF-8）。
  - 有「商品ID」= 更新，沒有 = 新增；「庫存」只用在新增的規格（已經有的規格要改庫存請用盤點）；檔案裡沒列到的規格不會被刪掉；沒有成本權限的員工匯出的表沒有「成本」欄，也改不到成本。
- **複製商品**：商品頁「複製」→ 建立一個草稿，規格和價格一樣，庫存 0、貨號清空、圖片要重新上傳。
- **自訂排序**：商品頁「調整排序」，拖曳或按 ↑ ↓，前台照這個順序顯示；新商品會排最前面。
- **排程上下架**：商品頁「排程上下架」可以設上架時間、自動下架時間（限時商品、預購、活動檔期）。時間沒到或過了，前台自動看不到、也不能下單；後台標示「排程中」「已下架（到期）」。

## v0.14 新增：出貨與訂單工具

- **勾選訂單批次處理**：訂單列表每列前面有勾選框（可全選這一頁，換頁也會記得）。上方會出現：
  - **列印出貨單**：一筆一張 A4，有收件人、取貨方式與門市／地址、付款（取貨付款會大字印「取貨收款 NT$…」）、商品清單附打勾欄、備註。可以選要不要印價格（送禮的訂單建議不印）。
  - **列印揀貨單**：把勾選訂單要撿的商品彙總成一張，依商品排序，每個規格總數量、出現在哪幾筆訂單。
  - **確認收款**、**標記已出貨**（每筆可以各自填物流單號）、**標記已完成**：一次改很多筆；改不了的（例如已取消）會列出來，其他照樣完成。
- **單筆訂單**也可以列印出貨單。
- **退貨／退款紀錄**：已付款的訂單可以記錄退款，可以只退部分商品、部分金額，選擇退回的商品要不要加回庫存（庫存異動記為「退貨入庫」）。可以退很多次，系統會擋下超過的數量和金額。訂單頁顯示「已退款、實收」，列表標示「退 NT$…／全額退款」，顧客查單看得到退款總額。
- 退款會自動算進：**報表**（實收、毛利扣掉退款、退貨加回的成本）與**會員累積消費**（影響會員等級）。
- 金流還沒串接，錢要商家自己退給顧客，這裡是記錄。

## v0.13 新增：員工帳號與權限

後台「商店 → 員工」（只有店主看得到）。

- **邀請員工**：填員工的 Email、勾選權限 → 產生**邀請連結**，用 LINE 或 Email 傳給對方。對方打開連結、用被邀請的 Email 登入或註冊（確認信點完會自動回到邀請頁），按「加入」就好。連結 7 天內有效、只能用一次，可以取消。
- **7 種權限**：訂單、會員、商品、進銷存、行銷、報表、設定。員工只看得到有權限的選單；直接打網址也會顯示「沒有權限」，資料庫那邊也會擋下。
- **成本保密**：沒有「進銷存」或「報表」權限的員工，看不到任何成本（商品頁、訂單都不會出現），改商品時也不會動到成本。
- **店主可以隨時改權限、移除員工**；員工可以在「我的商店」自己離開。每家店最多 20 位成員。
- **記下是誰做的**：訂單紀錄（確認收款、出貨、取消）和庫存異動會顯示操作者的 Email。

## v0.12 新增：外觀（佈景主題）

後台「商店 → 外觀」，右邊有即時預覽，按「儲存外觀」後前台馬上換。

- **配色**：暖米、森林、海洋、玫瑰、暖陽、墨黑六組，每組都有另外設計的深色模式；也可以**自訂主色**（按鈕、連結、橫幅），系統會自動選按鈕文字用白或黑、算出深色模式用的亮一點的版本，顏色太淡會提醒
- **Logo**：上傳 PNG／JPG／WebP，保留透明背景，前台縮成 40px 高；深色模式會自動墊淺色底
- **首頁版型**：簡潔格狀、大橫幅（主色橫幅＋標語＋「逛逛商品」按鈕）、雜誌風（第一個商品放大介紹）；可自訂首頁標題與說明
- **商品排列**：每列 2／3／4 個（手機固定 2 個）、圖片方形 1:1 或直式 4:5
- **字體**：黑體（俐落）或明體（典雅）
- **公告列**：最上方自訂一句公告，會和滿額活動、免運門檻一起顯示
- 資料庫版會檢查設定內容（例如 Logo 只能用自己商店的圖片）

## v0.11 新增：銷售／毛利報表

後台「銷售 → 報表」。期間可選近 7 天、近 30 天、本月、上個月、近 90 天、今年或自訂（最多 3 年）。

- **四個重點數字**：營收（只算已付款：待出貨、已出貨、已完成）、毛利與毛利率、平均客單、購買顧客（新客／回購）
- **金額怎麼來的**：商品金額 − 折扣 − 商品成本 ＝ 毛利，＋ 運費收入 ＝ 營收；待付款、已取消另外列出，不算進營收
- **營收趨勢圖**：62 天以內每天一根、更長每月一根；滑過（手機點一下）顯示營收、毛利、訂單數；下方有可展開的明細表
- **熱銷商品**（銷售額、成本、毛利、毛利率、占營業額）、**熱銷規格**、**付款方式**、**取貨方式**、**折扣來源**（滿額、會員等級、各優惠碼）
- **進貨花費**：期間內入庫的件數與金額，依供應商分
- **匯出 Excel**：摘要、趨勢、商品、規格、付款與折扣、進貨六張工作表
- 成本用的是「下單當時」的平均成本，之後改進價不會影響過去的毛利；資料庫版由伺服器計算全部訂單

## v0.10 新增：上線前防護、訂單分頁

- **防灌單**（伺服器檢查，繞不過）：
  - 同一支手機在同一家店：10 分鐘最多 5 筆、一天最多 20 筆；還有 5 筆沒付款就不能再下單
  - 同一個 IP：10 分鐘最多 20 筆（換手機也算）
  - 整家店：1 小時最多 300 筆
  - 結帳頁有一個真人看不到的欄位，機器人填了就擋下
- **防猜訂單**：訂單查詢打錯手機只會說「查不到」；同一筆訂單 10 分鐘最多查 10 次、同一個 IP 最多查 30 次。
- **逾期未付款自動取消**：後台「設定」可以填「銀行轉帳幾天沒付款自動取消」（預設 0 = 不取消）。取消後庫存加回、訂單紀錄寫明原因，避免假訂單卡住庫存。
- **訂單多了也不會變慢**：後台只先載入最近 120 天和還沒結案的訂單；訂單頁改成一頁 50 筆、由資料庫搜尋全部訂單（含很久以前的）；匯出 Excel 也是從資料庫拿全部符合的訂單。會員的累積消費、各狀態數量都算全部訂單。會員列表一次顯示 100 位，可以再展開。

## v0.9 新增：商家自助開店＋平台總控台

- **商家自己開店**：平台首頁按「免費試用」→ 用 Email 註冊 → 點確認信 → 填商店名稱與網址代號（3～30 個小寫英文、數字、減號）→ 馬上有自己的店：`…/?store=代號`。新商店的取貨方式、付款方式都先幫忙設好。
- **試用與年費**：新店自動試用（預設 14 天）。到期或被平台停用時，前台顯示「暫停營業」、不能下單，但顧客仍可查舊訂單；商家還能登入後台看資料。後台最上方會提醒「還剩幾天」「已到期」，設定頁有「方案與年費」和你的商店網址。
- **一個帳號可以有好幾家店**：後台左下「我的商店・開新店」可以切換或再開一家（預設每個帳號最多 3 家）。
- **平台總控台**（`#platform`）：
  - 總覽：商店數、試用中、30 天內到期、今年年費收入、各店近 30 天營收
  - 商店列表：依狀態分頁（試用中／年費有效／30 天內到期／已到期／已停用／免費）、搜尋
  - 單一商店：登記收到年費（自動延長一年，提早續約會接在原到期日後面）、調整到期日、改方案、停用／恢復（要填原因，商家看得到）、平台備註（商家看不到）
  - 收款與異動紀錄：每一筆都記下是誰操作
  - 平台設定：平台名稱、年費、試用天數、每個帳號可開店數、是否開放自助開店、聯絡 Email／LINE
- **前台頁尾**加上「我也要開店」連到平台首頁。
- **修正**：v0.8 正式版的後台「設定」頁打不開的問題。

## 從 v0.8／v0.9 升級（照順序做）

1. **Supabase → SQL Editor → New query**：貼上新的 `supabase/schema.sql` 整份，按 Run，看到「完成：資料庫是 v0.21 版」。原本的資料都會保留；原本的範例商店會變成「免費方案」，不會到期。
2. **把自己設成平台管理者**（v0.9 已經做過就跳過）：在 SQL Editor 再執行一行（換成你註冊後台用的 Email）：
   ```sql
   select setup_platform_admin('你的email');
   ```
3. **GitHub**：用 Upload files 把 `index.html`、`demo.html`、`README.md`、`css`、`js`、`supabase` 六樣拖進去覆蓋，按 Commit changes。
4. 打開 `…/#platform` → 「平台設定」填上聯絡 Email（商家要續約時會看到），確認年費與試用天數。

> 以前的商店網址 `…/#shop` 還是可以用（會打開範例商店），但建議改用 `…/?store=demo`。

## v0.8 新增：接上資料庫（第二階段）

第一階段先隱藏的功能，現在正式版全部打開、資料都存在資料庫。

- **顧客會員（Email＋密碼）**：前台註冊填 Email、姓名、手機、密碼 → 收確認信 → 點信裡的連結就回到網站、自動完成註冊。有「忘記密碼」（寄重設信）、會員中心可以改姓名／手機、變更密碼。
- **舊訂單自動接上，但只認 Email**：以前沒登入、用同一個 Email 下過的訂單，註冊並確認信箱後會自動接到會員底下。只填同一支手機不會接上別人的訂單（防止冒用）。
- **會員等級**：後台「行銷 → 會員等級」設定，存在資料庫。結帳金額一樣由伺服器算：商品小計 → 滿額活動 → 會員等級折扣 → 優惠碼 → 運費。
- **商品圖片存雲端**：上傳到 Supabase Storage（`product-images`），不再受瀏覽器 5MB 限制。只有這家店的管理者能上傳、刪除；刪掉圖片或刪掉商品時，雲端檔案也會一起刪。每張圖最大 2MB（上傳前會自動縮小，一般照片不會超過）。
- **進銷存**：庫存總覽、盤點調整、庫存異動紀錄、進貨單（含部分入庫、移動平均成本）、供應商，全部改存資料庫。庫存帳和每一筆異動對得起來。
- **Email 連結**：確認信、重設密碼信的連結會帶 `?next=shop`／`?next=admin`／`?next=reset`，回到網站時自動登入並跳到對的頁面；連結過期或在別的瀏覽器打開，會提示該怎麼做。

## 從 v0.7 升級（照順序做）

1. **Supabase → SQL Editor → New query**：把新的 `supabase/schema.sql` 整份貼上，按 Run。看到「完成：資料庫是 v0.8 版」就好了。**原本的資料都會保留**（可以重複執行）。
   如果又跳出「Potential issues detected」，一樣按「Run and enable RLS」。
2. **Supabase → Authentication → URL Configuration**：確認 Redirect URLs 裡有 `https://shoplatform.github.io/shoplatform/**`（v0.7 設過就不用動）。
3. **Supabase → Authentication → Sign In / Providers → Email**：「Confirm email」保持**打開**（舊訂單接上靠的就是信箱驗證）。
4. **GitHub**：用 Upload files 把 `index.html`、`demo.html`、`README.md`、`css`、`js`、`supabase` 六樣拖進去覆蓋，按 Commit changes，等 1～2 分鐘。

> **正式開放給顧客前要做：自訂寄信（SMTP）**。Supabase 內建的寄信服務只會寄給「專案成員」的信箱，而且每小時只能寄幾封，只適合自己測試。要讓一般顧客收得到確認信，要到 **Authentication → Emails → SMTP Settings** 接上寄信服務（例如 Resend、Brevo，有免費額度）。在那之前，可以先用你自己的 Email 測試會員註冊。

## 第一次設定（全新的 Supabase 專案才需要）

1. **Supabase → Authentication → URL Configuration**：Site URL 填 `https://shoplatform.github.io/shoplatform/`；Redirect URLs 加一筆 `https://shoplatform.github.io/shoplatform/**`。
2. **Supabase → SQL Editor → New query**：把 `supabase/schema.sql` 整份貼上，按 Run。看到「完成：資料庫是 v0.8 版」就完成。
3. **把檔案上傳到 GitHub**（見下方），等 1～2 分鐘。
4. **打開網站的商家後台**（網址後面加 `#admin`）→「第一次使用？建立帳號」→ 用你的 Email 註冊 → 到信箱點確認連結（會直接回到後台）。
5. 畫面會出現「還差一步：設定店主」，照畫面把那一行指令貼到 SQL Editor 執行，回來按「重新檢查」就進得去了。

連線設定在 `js/config.js`：Supabase 網址、Publishable key（公開金鑰，本來就放在網頁裡）、商店代號。**不要把 Secret key／service_role 放進任何檔案。**

## v0.7：接上 Supabase 資料庫（第一階段）

- **資料放雲端**：商品、分類、設定、訂單、顧客、優惠券、滿額活動都存在 Supabase。手機下的單，電腦後台馬上看得到。
- **商家後台要登入**：Email＋密碼。只有被設成「店主」的帳號能進後台；別人就算註冊了也看不到任何資料。
- **金額由伺服器算**：購物車、結帳的金額、滿額折、優惠碼都由資料庫計算；下單時重新檢查庫存、價格、優惠碼，瀏覽器送來的價格一律不採用。
- **不會超賣**：同時很多人搶最後幾件，只有庫存夠的訂單會成立（測過 8 人搶 3 件，剛好成立 3 張）。
- **改庫存不會蓋掉剛賣掉的**：商品頁改庫存時，只送「這次改了多少」，這段時間顧客買走的數量不會被覆蓋。
- **資料庫全部上鎖**：資料表不開放直接讀寫，只能透過 `supabase/schema.sql` 裡的函式；前台函式只回公開資訊（不含成本、商家備註、內部設定）。

## v0.6 新增（進銷存）

- **庫存**：後台「進銷存 → 庫存」。每個規格一列：可售庫存、在途（已下單還沒到貨）、平均成本、庫存成本；上方有規格數、庫存件數、庫存總成本、庫存偏低數。可以篩選偏低／缺貨、匯出 Excel。
- **盤點調整**：每一列按「調整」，可以填「盤點後的實際數量」或「增減數量」，要選原因（盤點差異、損壞報廢、樣品／贈品、找回、其他）。庫存不會被調成負數。
- **庫存異動紀錄**：每一次庫存變動都會記下來：銷售、訂單取消加回、進貨入庫、盤點調整、在商品頁直接改、期初庫存。每筆有時間、增減、變動後剩多少、對應的訂單或進貨單（點了可以跳過去）、備註。可以看全部或只看某個規格。
- **進貨單**：草稿 → 已下單 → 部分入庫 → 已入庫，也可以取消。可以一鍵「帶入庫存偏低的規格」（已經算進在途數量，不會重複補），系統建議補到提醒數量的 3 倍。貨到了按「入庫」，可以只收一部分；剩下的不收了就按「剩下的不收了」結案。
- **平均成本**：入庫時用移動平均自動更新：（原庫存 × 原成本 ＋ 入庫數量 × 這次進價）÷ 新庫存。商品編輯頁也多了「平均成本」欄；訂單會記下當時的成本，之後可以算毛利。
- **供應商**：名稱、聯絡人、電話、Email、統一編號、地址、備註；可以看他的進貨紀錄、直接「向他進貨」。還有未入庫進貨單的供應商不能刪除。
- **修正**：手機寬度下後台選單太長時，整頁會被撐寬、要左右滑；現在只有選單列本身可以橫向捲動。

## v0.5 新增

- **會員分級**（藍圖 B3-02）：後台「行銷 → 會員等級」。最多 5 個等級，每級設升級門檻、折數（95 = 95 折）、要不要免運。消費只算「已付款、沒取消」的訂單；可以選「全部累積（只升不降）」或「最近 12 個月（會自動降級）」。
- **結帳怎麼折**：商品小計 → 滿額活動 → 會員等級折扣 → 優惠碼 → 運費。等級優惠要會員登入後結帳才會套用；沒登入時購物車會提醒可以登入享優惠。
- **會員中心**：顯示目前等級、權益、升級進度條，和「再消費多少升級」；可以展開看全部等級。
- **後台會員**：列表多一欄等級、可以依等級篩選；會員頁顯示有效消費和升級進度。
- **範例等級**：一般會員（0）、銀卡（滿 3,000，95 折）、金卡（滿 10,000，9 折＋免運）。示範帳號 0912345678 目前是銀卡。

## v0.4 新增

- **優惠券**（藍圖 B4）：後台「行銷 → 優惠券」。三種優惠：折固定金額、打折（85 代表 85 折，可設最多折多少）、免運。每張可以設使用門檻、開始／結束日期、總共可用次數、每位顧客可用次數（用會員帳號或下單手機計算）、限會員使用、能不能和滿額活動一起用、啟用開關。訂單取消後，次數會自動退回。
- **滿額活動**（藍圖 B4）：後台「行銷 → 滿額活動」。可以設好幾段，例如「滿 1500 折 150、滿 3000 折 400」，結帳時自動套用最划算的一段（不疊加）；可設期間與開關。
- **前台**：最上方公告列會顯示進行中的活動；購物車和結帳可以輸入優惠碼，不能用時會直接說原因（還差多少、已過期、限會員、已經用過…）；會提示「再買多少可以多折／免運」；免運門檻看的是折扣後的金額。
- **訂單**：折扣明細會記在訂單上，後台訂單頁、顧客的訂單頁都看得到；匯出 Excel 多一欄「優惠碼」。
- **範例資料**：`WELCOME100`（滿 800 折 100，每人一次）、`AUTUMN85`（滿 1000 打 85 折，最多折 300，不能和滿額活動併用）、`FREESHIP`（會員免運，滿 500）、`SUMMER50`（已過期，示範用）、「秋季滿額折」滿 1500 折 150／滿 3000 折 400。

## v0.3 新增

- **前台會員註冊／登入**（藍圖 A2-01）：帳號是手機號碼。註冊要先拿手機驗證碼——簡訊還沒串接，所以目前是「示範模式」，驗證碼直接顯示在畫面上。密碼至少 8 碼、要有英文和數字，只存加鹽雜湊（SHA-256），不存原文；密碼連錯 5 次暫停 5 分鐘。以前用同一支手機結帳過的顧客，註冊後會自動接上原本的訂單。
- **會員中心**：我的訂單、修改姓名與 Email、變更密碼、登出。登入後結帳會自動帶入會員資料，訂單直接掛在會員帳號下。
- **訂單查詢**（藍圖 A2-02）：不用登入，輸入訂單編號＋下單手機就能看訂單進度（待付款 → 備貨中 → 已出貨 → 已完成）、物流單號、付款說明。顧客看不到商家備註；手機號碼會遮一部分。
- **顧客自行取消**：「待付款」的訂單顧客可以自己取消，庫存自動加回；已經在處理的訂單要聯絡客服。
- **下單完成頁**：可以直接「查看訂單」；還不是會員的話，提示「設定密碼成為會員」，按下去會帶入剛剛的手機和姓名。
- **後台會員頁**：顯示每位會員「已開通／未開通」帳號。
- **示範帳號**：手機 `0912345678`、密碼 `demo1234`（林雨潔）。

> 這一版也修掉一個問題：以前沒登入結帳時，會用 Email 去比對既有會員，打錯或借用別人的 Email 會讓訂單掛到別人名下。現在只用手機比對，而且不會覆蓋已開通會員自己維護的資料。

## v0.2 新增

- **商品圖片上傳**（藍圖 B1）：商品編輯頁可以點選或拖曳上傳，每個商品最多 8 張；第一張是主圖，可以「設主圖」、「移除」。圖片會在瀏覽器裡自動縮小（長邊 1200px）並轉成 JPEG，透明背景會變成白底。前台列表、商品頁（可切換多張圖）、購物車、後台列表都會顯示主圖；沒有圖片的商品照舊用色塊。
- **訂單匯出 Excel**（藍圖 B2-06）：訂單頁右上角「匯出 Excel」。依照目前的狀態分頁和搜尋條件匯出，可以再選「不限日期／本月／自訂日期」。下載的是真正的 `.xlsx`，有兩張工作表：「訂單」（一張訂單一列）與「訂單明細」（一個商品一列），標題列凍結、可篩選，金額有千分位，手機號碼開頭的 0 會保留。不需要任何外部套件。

> **圖片目前的限制**：圖片跟其他資料一起存在瀏覽器的 localStorage，大約只有 5MB，一般商品照片大概能放 20～30 張。空間不夠時會跳出提示、不會存到一半。等換成真正的後端（階段 2），圖片會改存雲端，這個限制就沒了——只需要改 `js/db.js` 裡的 `DB.media.prepare()`。

## 現在能做什麼

**商家後台**（`#admin`）
- 總覽：今日／本月營收、待出貨、會員數、近 14 天營收圖、低庫存提醒、最新訂單
- 商品：新增、編輯、刪除、上架／草稿、多規格（例如 顏色 × 尺寸 自動組合）、每個規格各自價格與庫存、**商品圖片**
- 分類：新增、改名、刪除
- 訂單：依狀態分頁、搜尋、訂單詳情、手動確認收款 → 出貨（填物流單號）→ 完成、取消（庫存自動加回）、**匯出 Excel**
- 會員：結帳時自動建立、累積消費、訂單紀錄
- 行銷：優惠券、滿額活動（v0.4）、會員等級（v0.5）
- 進銷存：庫存總覽與盤點調整、庫存異動紀錄、進貨單（含部分入庫）、供應商、平均成本（v0.6）
- 設定：商店資料、免運門檻、低庫存提醒、取貨方式與運費、付款方式開關、重置範例資料

**商店前台**（`#shop`）
- 會員註冊／登入、會員中心、訂單查詢（v0.3）
- 優惠碼、滿額折、滿額提示（v0.4）
- 會員等級與等級折扣（v0.5）
- 首頁、分類、搜尋、商品頁（選規格、看庫存、數量、**多張圖片**）
- 購物車（免運差額提示）
- 結帳（訂購人、超商取貨或宅配、付款方式、備註）→ 下單完成頁
- 下單會自動扣庫存、建立會員、出現在後台

**刻意還沒做的**（照藍圖排在後面）
- 金流、物流、電子發票串接 → 目前付款與出貨都由商家在後台手動確認
- 顧客確認信的自訂寄信（SMTP，上線前設定）
- 離線示範版的手機驗證碼仍是示範模式
- 導購分潤、多商家註冊、年費計費

## 怎麼在自己電腦上打開

- 正式版 `index.html` 需要網路，會連到 Supabase。
- 離線示範版：直接用瀏覽器打開 `demo.html` 就可以。
上方黑色的 **DEV** 列可以在「商家後台」和「商店前台」之間切換。

資料存在瀏覽器的 localStorage：換瀏覽器或清除網站資料就會回到範例資料。
後台「設定 → 重置範例資料」也可以手動還原。舊版留下的資料會自動升級，不用重置。

## 放上 GitHub（GitHub Pages 免費網址）

1. 在 GitHub 建一個新的 repository，例如 `shop-platform`（Public 或 Private 都可以；Pages 免費版需要 Public）
2. 把這個資料夾的檔案上傳：
   - 最簡單：在 repository 頁面按 **Add file → Upload files**，把整個資料夾內容拖進去，按 **Commit changes**
   - 或用指令（這個資料夾已經是 git 專案）：
     ```bash
     git remote add origin https://github.com/<你的帳號>/shop-platform.git
     git branch -M main
     git push -u origin main
     ```
3. 到 repository 的 **Settings → Pages**，Source 選 **Deploy from a branch**，Branch 選 `main`、資料夾選 `/ (root)`，按 Save
4. 等 1～2 分鐘，網址會是 `https://<你的帳號>.github.io/shop-platform/`

已經放過舊版的話：用 **Upload files** 把 `index.html`、`demo.html`、`README.md`、`css`、`js`、`supabase` 拖進去覆蓋即可。

## 檔案結構

```
shop-platform/
├── index.html          正式版入口（連資料庫）
├── demo.html           離線示範版入口（資料存在瀏覽器）
├── supabase/
│   └── schema.sql      資料庫設定：資料表、權限、所有伺服器函式（貼到 SQL Editor 執行）
├── css/
│   ├── base.css        共用色彩、按鈕、表單、表格（含深色模式）
│   ├── admin.css       商家後台版面
│   └── shop.css        商店前台版面（之後的「佈景主題」從這裡長出來）
├── js/
│   ├── config.js       連哪個 Supabase 專案、哪家商店
│   ├── vendor/supabase.js   Supabase 官方程式庫（@supabase/supabase-js 2.117.2）
│   ├── db.js           資料層（資料庫版）
│   ├── db-local.js     資料層（離線示範版）
│   ├── seed.js         離線示範版的範例資料
│   ├── ui.js           共用工具：金額格式、提示訊息、確認視窗、網址路由、下載
│   ├── xlsx.js         產生與讀取 Excel 檔（不靠外部套件）
│   ├── importer.js     商品 Excel 匯出／匯入規則（v0.15）
│   ├── admin.js        商家後台所有頁面（含登入）
│   ├── shop.js         商店前台所有頁面
│   ├── platform.js     平台首頁、平台總控台、方案狀態（v0.9）
│   ├── theme.js        佈景主題：配色、版型、字體（v0.12）
│   └── app.js          啟動：先載入資料，再依網址顯示後台或前台
├── AGENTS.md           給 AI 程式助理（Codex 等）的開發守則
├── docs/藍圖.md        產品藍圖與交接文件（現況、架構、接下來要做什麼）
└── test/               本機測試：模擬 Supabase＋資料庫測試（cd test && npm install && bash run-all.sh）
```

## 設計原則

- **頁面只跟 `DB` 說話**：所有讀寫都經過 `window.DB`。資料庫版（`db.js`）和離線版（`db-local.js`）提供一樣的介面：讀取從快取同步拿，寫入是非同步、成功後重新載入。
- **多租戶從第一天就在**：資料結構是 `stores[商店ID]`，現在只有一家示範店，之後開放多商家時不用改資料形狀。
- **不用任何框架或打包工具**：純 HTML / CSS / JavaScript，任何人打開就看得懂，也方便用 AI 一段一段加功能。

## 下一步建議（對照功能藍圖）

| 階段 | 項目 | 藍圖編號 | 狀態 |
|---|---|---|---|
| 1b | 商品圖片上傳 | B1 | ✅ v0.2 |
| 1b | 訂單匯出 Excel | B2-06 | ✅ v0.2 |
| 1b | 前台會員登入、訂單查詢 | A2-01、A2-02 | ✅ v0.3 |
| 2 | 換成真正的後端與資料庫 | D1-01 | ✅ v0.7 第一階段、v0.8 第二階段（會員、圖片雲端、進銷存） |
| 2 | 優惠券、滿額活動 | B4 | ✅ v0.4 |
| 2 | 會員分級 | B3-02 | ✅ v0.5 |
| 2 | 上線前防護（防灌單、分頁） | — | ✅ v0.10 |
| 2 | 銷售／毛利報表 | — | ✅ v0.11 |
| 3 | 外觀／佈景主題 | P2 | ✅ v0.12 |
| 2 | 串接綠界金流、物流 | D1-02、D1-03 | |
| 2 | 商家自助註冊、平台總控台 | C1 | ✅ v0.9 |
